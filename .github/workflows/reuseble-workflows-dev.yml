name: Reusable workflow DEV

on:
  workflow_call:

jobs:
  build-dev:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Running on dev"

  test-dev:
    runs-on: ubuntu-latest
    steps:
      - run: |
        echo "World"